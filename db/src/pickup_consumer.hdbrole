{
    "role": {
        "name": "pickup_consumer",
        "schema_privileges": [
            {
                "privileges": [
                    "SELECT",
                    "SELECT CDS METADATA",
                    "SELECT METADATA"
                ]
            }
        ]
    }
}